new tasks, please pick one start ASAP so we do not need to hurry as before: Person A: Consistency Lead (Database Replicas)
Focus: state consistency and synchronization across 3 DB instances.
Coding:
Implement key-value store operations (Read/Write).
Implement replication protocol (for example primary-backup).
Ensure writes on primary are propagated and acknowledged by backups.
Define read behavior (from primary only, or safe replica reads if you support it).
Deliverables:
Working gRPC Books Database service running as 3 instances.
Consistency protocol diagram (replicas + executor + message flow).
Logs showing write propagation and replica state consistency.

Person B: Commitment Lead (Coordinator in Executor)
Focus: all-or-nothing transaction control in executor.
Coding:
Implement coordinator logic for 2PC (recommended for scope): Prepare -> collect votes -> Commit/Abort.
Add timeout and failure decision paths (abort on timeout/no vote).
Track transaction IDs and coordinator state transitions.
Deliverables:
Transaction coordinator logic in executor service.
Distributed commitment sequence diagram (Executor, DB, Payment).
Logs for Phase 1 and Phase 2 decisions.

Person C: Integration & Participants Lead (Payment + Participant Logic + Docker)
Focus: participant behavior and end-to-end integration/demo readiness.
Coding:
Create payment gRPC service as 2PC participant.
Implement participant-side Prepare/Commit/Abort in payment service.
Implement participant-side Prepare/Commit/Abort handling in DB service interface.
DevOps/Project:
Own docker-compose integration (3 DB replicas + executor + payment + existing needed services).
Ensure startup reliability and clear service connectivity.
Final README integration notes, cleanup, and checkpoint-3 tagging process.
Deliverables:
Working payment participant service.
End-to-end docker-compose demo flow.
Clean project organization for evaluation.